# ============================================
# TAUFIQ'S LOCAL ENVIRONMENT CONFIGURATION
# ============================================
# Use this when running the application on Taufiq's machine
#
# IMPORTANT:
# - Taufiq database → localhost (your local PostgreSQL)
# - Other databases → Remote IPs or SSH tunnels
# ============================================

APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

# ============================================
# DATABASE CONNECTIONS
# ============================================

# EILYA - Stray Reporting (REMOTE)
# Replace with Eilya's machine IP or SSH tunnel
DB1_HOST=EILYA_IP_ADDRESS_HERE
DB1_PORT=3307
DB1_DATABASE=workshop2
DB1_USERNAME=root
DB1_PASSWORD=

# ATIQAH - Shelter Management (REMOTE)
# Replace with Atiqah's machine IP or SSH tunnel
DB2_HOST=ATIQAH_IP_ADDRESS_HERE
DB2_PORT=3308
DB2_DATABASE=workshop
DB2_USERNAME=root
DB2_PASSWORD=

# SHAFIQAH - Animal Management (REMOTE)
# Replace with Shafiqah's machine IP or SSH tunnel
DB3_HOST=SHAFIQAH_IP_ADDRESS_HERE
DB3_PORT=3309
DB3_DATABASE=workshop
DB3_USERNAME=root
DB3_PASSWORD=

# DANISH - Booking & Adoption (REMOTE)
# Replace with Danish's machine IP or SSH tunnel
DB4_HOST=DANISH_IP_ADDRESS_HERE
DB4_PORT=1434
DB4_DATABASE=workshop
DB4_USERNAME=sa
DB4_PASSWORD=

# TAUFIQ - User Management (LOCAL - YOUR DATABASE)
# This runs on YOUR machine (localhost)
DB5_HOST=127.0.0.1
DB5_PORT=5434
DB5_DATABASE=workshop
DB5_USERNAME=postgres
DB5_PASSWORD=

# Backup Database (LOCAL)
BACKUP_DB_HOST=127.0.0.1
BACKUP_DB_PORT=5432
BACKUP_DB_DATABASE=backup_system
BACKUP_DB_USERNAME=postgres
BACKUP_DB_PASSWORD=password

# ============================================
# DEVELOPMENT WORKFLOW FOR TAUFIQ
# ============================================
#
# 1. Start your local PostgreSQL database
# 2. Run migrations for YOUR database only:
#    php artisan db:fresh-one taufiq --seed
#
# 3. When working with full application:
#    - Make sure SSH tunnels to remote databases are active
#    - OR ensure remote databases are accessible via network
#    - OR work offline (some features will be limited)
#
# 4. To work offline (only your database):
#    - Controllers will gracefully handle offline databases
#    - Only User Management features will work fully
# ============================================

TOYYIBPAY_KEY=
TOYYIBPAY_CATEGORY=

SESSION_DRIVER=file
SESSION_LIFETIME=120

CACHE_STORE=file
QUEUE_CONNECTION=database
